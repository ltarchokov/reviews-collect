# Установка (из каталога scripts/ внутри скилла):
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# Системный Python на macOS закрыт для установки пакетов (PEP 668),
# поэтому зависимости живут в .venv рядом с этим файлом, а скрипты
# запускаются через .venv/bin/python — не через python3.

# Результат отдаётся коллегам в Excel: сводка, отзывы, негатив, карточки
# в одной книге с фильтрами и закреплённой шапкой.
openpyxl>=3.1

# Подставляет TLS-отпечаток настоящего браузера. Без этого card.wb.ru
# отдаёт 403 на любые заголовки: площадки узнают requests по набору
# и порядку шифров, а не по User-Agent. Замерено на WB.
curl_cffi>=0.16

# Разбор HTML для площадок тира B (Ютека, iRecommend). Быстрый и без
# компиляции; BeautifulSoup здесь не нужен.
selectolax>=0.3
